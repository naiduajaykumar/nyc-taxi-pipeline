taxi_transform:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: postgres
      password: admin
      dbname: taxi_warehouse
      schema: public
      threads: 4